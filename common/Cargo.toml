[package]
name = "common"
version = "0.1.0"
edition = "2024"
authors = ["Pinhole Team"]
repository = "https://github.com/RafeLoya/pinhole"
description = "Shared protocol and utilities for terminal-based video call service"

[dependencies]
chrono = "0.4.40"
rand = "0.9.2"